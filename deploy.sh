sudo apt-get update && sudo apt-get upgrade
sudo apt-get install vim tmux most virtualenv zsh python-pip git

# in case the repo was not cloned at home
mv ./.zsh ~/.zsh

# deploy .zshrc
mv ./zshrc ~/.zshrc

