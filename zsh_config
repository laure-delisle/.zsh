# Aliases
[ -f ~/.zsh/aliases ] && source ~/.zsh/aliases

# Colors, ls and prompt
[ -f ~/.zsh/colors_prompt ] && source ~/.zsh/colors_prompt

# Completion
[ -f ~/.zsh/completion ] && source ~/.zsh/completion

# PATH
[ -f ~/.zsh/env_vars ] && source ~/.zsh/env_vars

